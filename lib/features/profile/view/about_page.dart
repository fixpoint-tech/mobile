// About page removed — placeholder kept to avoid accidental import errors.
import 'package:flutter/widgets.dart';

class RemovedAboutPage extends StatelessWidget {
  const RemovedAboutPage({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
